export type ProjectType = {
    contentPermissions: string;
    createdAt: string;
    description: string;
    id: string;
    name: string;
    owner: {
        id: string;
    };
    updatedAt: string;
}
